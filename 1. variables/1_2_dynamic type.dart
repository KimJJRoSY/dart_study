void main() {
  // dynamic name;
  var name;
  // name = 12;
  // name = 'rrr';
  // name = true;

  // if (name is String){
  // 이렇게 하면 name 변수 타입 선언 or 값 할당 안해도 문자열인지 앎
  //   name.
  // }
}

// dynamic 여러가지 타입을 가질 수 있는 변수에 쓰는 키워드 
// var name; <- 이렇게 변수 선언했을 때 다이나믹으로 봄 

// dynamic 사용할 때
// => 변수가 어떤 타입일지 알기 어려운 경우
